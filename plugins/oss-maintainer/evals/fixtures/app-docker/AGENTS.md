# Example App — agent rules

Commands: `make test`, `make lint`. Start the stack with `make start-all`.

Maintainer profile lives in `.maintainer/`; do not run release, triage or discussions workflows without it.
