---
type: llm
target: transcript
---

The transcript executes the current run_record.py commands (`new`, `set`, `show`), demonstrates the dependency-specific outcomes (a replaced digest resets only the artifact-bound check; `set --commit` resets source-bound checks and clears digests), uses `--reuse-from` with a rationale so the original check's identity, evidence and timestamp are preserved, records no fictional execution, and performs no external action.
