---
type: llm
target: trace
criteria: "The agent collects credits using the existing baseline tag and candidate SHA without trying to resolve or create a future tag. The owner's manual checks in the coverage table use the prepared candidate. Registry-dependent checks stay not-run until step 6 (verify and close), required for delivery but not prerequisites to publication. No unrun check is marked passed and no tag/publish action occurs."
---

The release contract is followed against the fresh fixture.
