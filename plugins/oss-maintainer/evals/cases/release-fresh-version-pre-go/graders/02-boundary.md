---
type: llm
target: trace
criteria: "The agent collects credits using the existing baseline tag and candidate SHA without trying to resolve or create a future tag. The pre-GO bucket C uses the prepared candidate. Registry-dependent checks stay pending for phase 11, required for completion but not prerequisites to publication. No unrun check is marked passed and no tag/publish action occurs."
---

The release contract is followed against the fresh fixture.
