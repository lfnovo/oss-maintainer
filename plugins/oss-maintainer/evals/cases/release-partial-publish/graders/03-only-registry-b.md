---
type: llm
target: last_message
criteria: "The agent recognises that registry-a already passed publication and does not repeat it, and identifies registry-b as the outstanding publication step, together with the remaining step 6 work (registry verification, announcement, cleanup, then finish; the retro is optional). The legacy record's phases are read as history, not re-executed."
---

Only the failed registry is retried.
