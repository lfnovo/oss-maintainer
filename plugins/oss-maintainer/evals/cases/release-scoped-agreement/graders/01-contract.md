---
type: llm
target: transcript
---

The answer keeps the tests approval valid under its written conditions (it survives the candidate change), invalidates the publication approval bound to the old candidate along with source-bound evidence unless reuse is justified in writing, respects the chosen direct-commit process, uses only discovered commands, and separates required delivery work (publish, verify) from optional retrospective work.
