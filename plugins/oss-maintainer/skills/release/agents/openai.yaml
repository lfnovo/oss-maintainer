interface:
  display_name: "Release"
  short_description: "Orchestrate a release behind human gates, from changelog audit to retro"
  default_prompt: "Use $release to prepare, test, cut and publish a release of this repository."
policy:
  allow_implicit_invocation: false
