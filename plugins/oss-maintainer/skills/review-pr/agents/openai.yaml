interface:
  display_name: "Review PR"
  short_description: "Review a pull request against the project's own rules; the maintainer decides what to post"
  default_prompt: "Use $review-pr to review a pull request of this repository."
policy:
  allow_implicit_invocation: false
