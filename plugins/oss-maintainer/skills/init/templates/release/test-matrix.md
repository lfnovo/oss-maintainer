# Coverage table

The risks this repository's releases keep checking, as a starting point for each release's
coverage table. The `release` skill instantiates it against the actual diff, keeps the
canonical validator and mandatory checks, and adds the risk-selected probes. Use commands
and selectors the repository supports; no secret values belong here.

| Check | Change / risk | Real probe | Observable success | Prerequisites | Who / paid scope | Stage | Mandatory |
|---|---|---|---|---|---|---|---|
| TODO | TODO | TODO | TODO | TODO | TODO | pre-GO | yes |

The project's inverse question, checked on every release (for example: does the protection
break legitimate use? does the abstraction hold parity across providers?):

- TODO
